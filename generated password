import string
import random


long = int(input("ingrese numeros de caracteres :"))

caracteres = string.ascii_letters + string.digits + string.punctuation

contra = "".join(random.choice(caracteres)for i in range(long))

print("la contraseña generada es :"+ contra)
